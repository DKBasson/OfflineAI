{
  "scripts": {
    "test": "python -m pytest",
    "test:ui": "playwright test",
    "build:ui": "cd react-app && npm run build",
    "dev:ui": "cd react-app && npm run dev",
    "test:react": "cd react-app && npm test"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0"
  }
}
